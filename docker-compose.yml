version: '3.8'

services:
  backend:
    image: rahma324ssalah/unique-x-backend:latest
    container_name: unique-x-backend
    restart: always
    ports:
      - "5000:80"
    environment:
      # ركزي هنا: غيرنا الـ IP لـ IP السيرفر واسم الداتابيز لـ Betk
      - ConnectionStrings__DefaultConnection=Server=37.60.230.225;Database=Betk;User=sa;Password=ABDO@menna2026;TrustServerCertificate=True;

  frontend:
    image: rahma324ssalah/unique-x-frontend:latest
    container_name: unique-x-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend